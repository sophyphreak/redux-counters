import React from 'react';

const divStyle = {
  textAlign: 'center',
  marginTop: '2%'
}

const TitleBar = () => (
  <div style={divStyle}>
    <h1>redux-counters</h1>
    <h4>created by Andrew Horn</h4>
  </div>
);

export default TitleBar;